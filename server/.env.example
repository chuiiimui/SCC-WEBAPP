# Supreme Court Cases API Environment Configuration
# Copy this file to .env and configure with your actual values

# Server Configuration
PORT=9090
<<<<<<< HEAD
NODE_ENV=development

# Session Security (REQUIRED)
# Generate a secure random string for production
# Example: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
=======
HOST=0.0.0.0
NODE_ENV=development
BASE_URL=http://localhost:9090

# Session Security (REQUIRED - MUST be set in production)
# Generate a secure random string for production using: openssl rand -base64 32
# The server will fail to start in production if this is not set to a secure value
SESSION_SECRET=dev-session-secret
>>>>>>> 4f727c7 (...//D//)

# CORS Configuration
# Frontend application URL that should be allowed to access the API
CORS_ORIGIN=http://localhost:5173
